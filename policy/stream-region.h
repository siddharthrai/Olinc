#ifndef STREAM_REGION_H
#define STREAM_REGION_H

#include "../common/intermod-common.h"

ub4 add_region_block(ub1 src_stream, ub1 straem, ub8 address);
ub4 get_xstream_reuse_ratio(ub1 src_stream, ub1 stream, ub8 address);

#endif
